#include <stdio.h>

/**
 * main - main block
 *
 * Description: combinations of single numbers
 *
 * Return: 0
 *
 */

int main(void)
{
	int g;

	for (g = 0; g < 10; g++)
	{
	putchar(g);
	putchar(',');
	putchar(' ');
	}
	return (0);
}
